# 05-data-model — Verification

## Acceptance criteria
- [x] `libs/db` is a workspace member; `uv sync --dev` resolves it.
- [x] `Base.metadata` creates all five tables on a fresh SQLite engine without error.
- [x] Company→Event→WebcastSource→Recording→TranscriptSegment graph can be inserted and navigated via relationships.
- [x] `Company.ticker` uniqueness is enforced (duplicate insert raises IntegrityError).
- [x] `EventStatus` and `RecordingStatus` enum values round-trip through the DB.
- [x] `session_scope()` commits on success and rolls back on exception.
- [x] `common.Settings` exposes `database_url` with a sane default.
- [x] The Alembic initial migration applies cleanly (`upgrade head`) to a throwaway DB and produces the model tables.
- [x] `make lint` and `make test` pass.

## How to verify (runnable)
1. `uv run pytest libs/db -q` → Expected: all tests pass, exit 0.
2. Alembic test applies `upgrade head` to a temp SQLite DB → Expected: the five tables exist afterward.
3. `uv run ruff check . && uv run ruff format --check .` → Expected: "All checks passed!".

## Evidence
```
$ uv run pytest -q
........................                      [100%]
24 passed in 2.82s        # includes libs/db: 5 model tests + 1 migration-apply test

$ uv run ruff check .  →  RUFF CLEAN
$ uv run ruff format --check .  →  22 files already formatted

# Alembic autogenerate detected all 5 tables + indexes; test_migration.py applies
# `upgrade head` to a throwaway SQLite DB and asserts the 5 tables exist.
```
- Model + session tests written and watched fail first (RED) before implementation (TDD).
- Alembic version files are excluded from ruff (autogenerated).

## Sign-off
Verified by: in-session verification (commands above re-run clean)  ·  Result: PASS  ·  Date: 2026-07-01
